import 'package:flutter/material.dart';

class DetailsUserScreen extends StatelessWidget {
  const DetailsUserScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Container(),
    );
  }
}
